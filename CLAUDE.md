# FluentFlow Claude Entry

Follow `AGENTS.md`. This file is only a Claude-compatible pointer so agent
rules do not drift.

Default language: Chinese. Code, commands, variables, and commit messages use
English.

Quick routing:

- Start with `docs/context_index.md` for broad or ambiguous work.
- Read `docs/agent_task_brief.md` before workflow, backend state, auth, queue,
  Agent API, deployment, or multi-module changes.
- Follow `docs/git_checkpoint_workflow.md` for staging and commits.
- Never push, deploy, tag, release, or bump `VERSION` unless explicitly asked.
