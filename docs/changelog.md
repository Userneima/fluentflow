# FluentFlow Changelog

本文档记录 FluentFlow 面向使用者和维护者的版本变化。它和 `docs/product_growth_log.md` 的分工不同：

- `product_growth_log.md` 记录为什么改、做过哪些判断和取舍。
- `changelog.md` 记录这次改了什么、影响什么用户、上线时要注意什么。

每次有可感知的产品变化、部署变化、数据口径变化或破坏性调整时，都应该补一条。不要把还没落地的计划写成已发布能力。

## 记录格式

```md
## YYYY-MM-DD｜一句话版本主题

### 用户可见变化
- ...

### 维护者变化
- ...

### 数据 / 口径变化
- ...

### 注意事项
- ...
```

如果某一类没有变化，可以省略。注意事项里要写清是否需要重启后端、重新构建前端、迁移数据、更新环境变量或通知用户。

## Unreleased

后续尚未准备发布的变更继续记录在这里。只有已经落地、验证并准备对外说明的内容，才移动到具体版本段落。

### 用户可见变化

- 登录页新增 Google 登录入口：配置 Google OAuth 后，用户可以用 Google 账号进入 FluentFlow；同邮箱的既有账号会自动绑定，新账号创建仍受 `FLUENTFLOW_ALLOW_SIGNUPS` 或首个管理员 bootstrap 规则控制。
- Word 笔记导出改为生成原生 `.docx` 文件，不再使用 HTML 伪装 `.doc` 的兼容方案；标题、段落、列表和表格会转成 Word 原生结构，并统一使用苹方字体。
- Word 笔记导出会把正文里的截图 Markdown 引用嵌入为真实图片；截图 artifact 会沿用产品 API 的登录态和 client scope 获取，取图失败时保留可读 caption/link 降级，不会让整份文档导出失败。
- PDF 笔记导出改为浏览器原生打印管线：系统会从当前 Markdown 笔记生成独立白底打印页并打开打印对话框，用户可在系统打印中另存为 PDF；不再使用 `html2pdf` / `html2canvas` 截图导出。
- 修复本地转写模式下「多视频无法排队」的问题：以本地身份运行时，一次选择多个音视频会因认证校验被拒（`/queue/process` 返回登录要求）而排队失败，历史里也无法建出队列任务；现在多文件排队可正常提交并逐个处理。
- 音视频上传改为统一走后台队列并串行处理：无论逐个上传还是一次多选，后台一次只处理一个任务，其余显示在「排队中」等待，避免多个大文件同时本地转写抢占资源导致机器卡顿。单文件上传的实时进度相应改到「处理记录」页查看，不再在上传页内联显示。
- 修复删除处理记录后又重新出现的问题：删除任务时会同步清理本地缓存和最近活动，不再因为前端缓存与后端列表合并而把已删除的记录重新并回来。
- 批量上传改用真实上传进度：进度条显示实际已上传百分比，不再固定停在 2%；上传卡住或连接中断时会在约 2 分钟无响应后自动中止并给出「上传失败或中断，请重新提交」提示，不再无限停在「上传中」界面。
- 修复本机使用云端转录时提交任务被拒（记录一闪而过就消失）的问题：本机单用户模式现在对本地和云端转录都标记为本地执行，云端转录的上传不再因为缺少本地执行标记而被账号鉴权拦下（服务端仍校验请求确实来自本机，公网部署不受影响）。
- 长稿笔记默认模式改为「章节覆盖」：转录超过约 2 万字时，自动模式从「高保真」切换为「章节覆盖」——它按章节组织、拆分更克制，在真实评测中重要要点覆盖更全、编造更少（见 `docs/note_pipeline_evaluation.md`）。「高保真」仍可在设置里手动选择；短稿仍走「直接」模式。
- 长稿笔记生成明显更快：抽取证据/分章等相互独立的步骤改为并行执行，一段约 40 分钟的长稿高保真笔记实测从约 230 秒降到约 130 秒。
- 修复「章节覆盖」笔记的两个格式问题：正文不再泄漏内部证据编号（如 `（E038）`），标题也统一为规整的 `一、二、三 + x.y` 层级编号。

### 维护者变化

- 移除 AI「笔记模式规划器」：自动模式不再额外调用一次模型来判断该用哪种笔记模式，改为按转录长度直接决定（短→直接，长→章节覆盖）。相关结果字段 `note_mode_plan_*` 不再产生，前端已做缺省降级。
- 「章节覆盖」增强容错：某一段证据返回的 JSON 解析失败时会重试一次，仍失败则跳过该段并记录告警，不再让整篇笔记失败。

### 维护者变化

- 云工作区代理改为默认硬禁用：现在必须同时设置 `FLUENTFLOW_CLOUD_WORKSPACE_URL` 和显式开关 `FLUENTFLOW_ENABLE_CLOUD_WORKSPACE=1` 才会开启同源代理。只设地址不设开关时后端会忽略地址，并在启动日志里明确提示。此改动是为避免一个残留的 `FLUENTFLOW_CLOUD_WORKSPACE_URL` 环境变量把本地上传静默转发到云端、导致上传卡死。
- 新增 `GOOGLE_OAUTH_CLIENT_ID`、`GOOGLE_OAUTH_CLIENT_SECRET`、`GOOGLE_OAUTH_REDIRECT_URI` 和可选 `FLUENTFLOW_GOOGLE_OAUTH_SCOPES` 配置；Google 登录只请求 `openid email profile`，不持久保存 Google access token。
- 前端新增 `docx` 依赖用于浏览器端生成 WordprocessingML `.docx`，并移除首页对 `html2pdf` CDN 的依赖。
- `docs/word_export_format_reference.md` 改为记录原生 `.docx` / 原生 PDF 打印导出的当前合同、截图嵌入规则和不可取图降级边界。
- `LOCAL_EXECUTION_EXACT_PATHS`（`backend/core/request_scope.py`）补入 `/queue/process` 与 `/summarize-transcript-file`，使本地执行豁免覆盖队列提交路径，与 `/process` 一致；信任模型不变（仍要求 localhost + `X-FluentFlow-Execution-Target: local` 头）。前端 `frontend/src/routes/media-text.jsx` 将非游客音视频上传统一改走 `/queue/process`，交由单后台 worker 串行执行。

### 注意事项

- 本次队列改动需重启后端并重新构建前端（`npm run build:frontend`）后生效。不改变数据结构，旧任务记录可正常读取。

## v0.2.1｜2026-07-05｜Stability and export fixes

### 用户可见变化

- Word 笔记导出新增专用版式规则：整篇文档优先使用苹方字体，统一中文、英文和数字的字体槽位，并修复列表重复圆点、空白行过多和表格左列被裁切的问题。
- Agent 处理记录卡片在中等宽度下会继续把取消 / 查看结果等操作保持在右上角，不再因为断点过大而提前掉到标题下方。
- 飞书导出改为按飞书官方 Markdown/HTML 转文档块流程优先创建文档；表格会走 Feishu Table / TableCell 块，官方转换不可用时才降级为标签化列表，避免直接显示 `|---|` 表格源码。
- PDF 笔记导出改为独立白底打印版式，会从当前 Markdown 笔记正文重新渲染可读页面，不再直接捕获编辑器的暗色界面或屏幕布局。
- 编辑器新增“关键画面复查”区域：会展示适合回看图表、代码、界面、公式或流程的关键画面候选，并可点击时间点辅助对照学习；正文仍只显示已插入笔记的高置信截图。
- 编辑器重生笔记现在可以从已有转录结果恢复：当旧任务记录已被清理或不在当前账号作用域时，会创建新的重生任务，而不是直接返回 `Job not found`。
- 编辑器从轻量缓存打开结果时，如果完整逐段转录加载失败，会明确提示“只拿到了缓存预览”，不再把截断文本当作完整可编辑转录展示。
- 修复多文件/队列上传时本地转录设置没有随 `/queue/process` 请求一起进入本地执行通道的问题；后续选择“本地转录”的队列任务会正确走本地路线。
- 处理记录页会即时合并最近活动里的轻量任务记录，刚提交或刚刷新的任务不再等 `/jobs` 后台刷新后才出现在列表里。
- 处理记录页进行中任务的进度条会跨满整张记录卡片宽度，不再被右侧“取消”按钮挤短。
- 从最近活动打开编辑器时，轻量缓存结果会先显示“正在加载逐段转录”，不再短暂误报“当前结果没有时间戳分段”后才恢复正常分段列表。
- 最近活动卡片的状态标签会固定横向显示，避免窄卡片里“已完成”等状态被挤成竖排。
- 编辑器会在音频结果中隐藏“视频复查”模式切换，避免音频笔记出现不可用的视频复查入口；视频结果仍保留文本校对 / 视频复查切换。
- 编辑器的提示词模板弹窗改为左侧模板列表、右侧编辑器的工作面板布局，暗色模式表面、输入框和保存区更贴近当前产品界面；默认模板名称同步改为“通用学习笔记”。
- 处理记录和处理详情的材料判断不再把结构化分享/讨论类长视频默认叫成“课程材料”；系统会优先使用笔记策略 Agent 的材料类型，并把普通长视频兜底显示为更中性的学习材料，笔记合成阶段也不再把所有材料预设为“同一门课程”。
- 编辑器和处理详情页移除整段字幕自动纠错的提示、查看修正按钮和纠错详情模块；旧任务的底层纠错字段仍保留兼容，但不再作为主界面能力展示。
- 字幕纠错层改为默认关闭：新任务默认不再额外调用模型做字幕纠错，也不会把自动纠错结果直接用于笔记生成；如需继续实验，可通过环境变量显式开启。
- 课程笔记默认提示词不再强制套用固定五段模板；新生成/重生的笔记会先按材料本身的讲述结构组织章节，更适合课程、教程、访谈和长视频学习材料。
- 修复设置选择“本地转写”后，显式云端转写 allowlist 会把本机任务重新规整到 ElevenLabs 云端路线的问题；本机/本地执行请求现在会保留 local 路线，公开云端仍保持云端转写策略。
- 编辑器里的 AI 笔记失败提示不再原样展示上游 API JSON；百炼 / DashScope API Key 无效时会显示可理解的“更新 API Key 后重生笔记”提示，并保留“转录已保存”的语义。
- 处理记录页定时刷新任务时，云工作区列表代理遇到一次性响应断流会自动重试；默认任务和本地任务只要有一路刷新成功，就不会反复弹出“任务刷新失败”。
- 开始处理页和“视频转写与总结”的最近活动卡片不再先探测 `/jobs/{task_id}`；已缓存完成结果会直接打开编辑器，其他记录回到处理记录页，避免已删除或本地任务在控制台留下无意义的 404。
- 处理记录页的本地音视频“重新提交”会先检查后端是否声明支持源文件重提接口；旧后端会走兼容入队路径，避免按钮卡住并在控制台留下 `/jobs/{task_id}/retry` 405。
- 处理记录页的本地音视频“重新提交”改为服务器端复用已保存源文件直接创建新任务，不再先把大文件下载到浏览器再重新上传；按钮等待态也会显示“正在入队”。
- 进入产品页时的初始加载态不再显示“正在检查访问权限”，改为中性的 FluentFlow 打开提示；只有确实需要登录或访问码时才展示认证界面。
- “视频转写与总结”和开始处理页的最近活动卡片不再跳入旧的“处理详情”页面；完成记录会打开编辑器，未完成或失败记录会回到处理记录主页面。
- 修复本地产品页控制台里的 `favicon.svg` 404，并让云工作区代理下的普通 JSON API 不再以 chunked streaming 方式返回，减少 `/jobs` 刷新时的 `ERR_INCOMPLETE_CHUNKED_ENCODING` 报错。
- 处理记录页的“重新提交”不再只是跳到开始处理页：视频链接会直接按原链接创建新任务，本地音视频记录会在源文件仍可用时复用原文件重新入队；源文件已清理时会提示用户重新选择文件。
- 处理记录页在同一次前端会话里会复用上一份任务列表即时渲染，切到别的页面再返回时不再像冷启动一样等待整页重新加载；后台仍会静默刷新真实任务状态。
- 处理记录页会优先展示上传中、进行中和排队中的任务；多文件上传会尽快按每个文件显示独立进程卡，顶部批次提示只作为短暂辅助反馈。
- 处理记录页的失败、已取消和已完成记录新增“删除记录”动作，用户可以清理无价值的终态记录；进行中任务仍使用“取消”。
- 处理记录页精简失败/已取消等终态卡片，不再默认展开大块“当前阶段”详情面板；失败原因改为紧凑提示。
- 开始处理页和任务进度概览的当前任务取消动作新增二次确认，并统一使用更明确的终止任务图标。
- 修复处理记录页 `/agent` 和管理页 `/admin` 等前端页面强制刷新后被后端 API 路由吞掉、显示 `{"detail":"Not Found"}` 或登录墙的问题；这些页面现在会正确回到前端应用，`/agent/v1/...` 和 `/admin/...` API 仍保持受保护接口语义。
- 修复账号切换后的处理记录缓存隔离：新账号、旧账号、访客和本机缓存不再互相污染，前端不会把旧账号记录写入新账号的任务缓存；已被旧版本误写入当前账号缓存的其他账号/访客记录也会在读取时按任务 `client_id` 丢弃。
- 设置页新增飞书账号连接状态和连接/断开入口；飞书导出默认走用户 OAuth 连接，编辑器在未连接时会引导用户先连接自己的飞书账号，而不是把导出失败展示成普通系统错误。
- 官网首页右侧 mini browser 顶部步骤切换控件改为窄圆角进度条样式，当前步骤亮起、非当前步骤弱化，减少顶部重复文字。
- 修复官网首页右侧 mini browser 切换不同步骤时带动左侧 hero 文案上下波动的问题，让步骤切换只影响右侧演示内容。
- 修复官网首页右侧 mini browser 的 Input 和 Study / Review 步骤内容在桌面断点超出外框的问题，改为按步骤使用匹配的演示视口高度。
- 官网首页右侧 mini browser 的 Export 步骤重组为更像文件资产的导出清单，视频、音频、字幕、关键画面和笔记导出项会显示 `.mp4`、`.srt`、`.md`、`.pdf` 等格式后缀。
- 官网首页右侧 mini browser 的 Input 步骤在进度条下方补充来源判断标签框，让链接识别、长视频、字幕/转录和关键画面准备状态更接近真实处理流程。
- 官网首页右侧 mini browser 的 Study / Review 来源锚点在暗色模式下改为更清晰的深底与青绿色锚点色，避免黄字黄底糊在一起。
- 官网首页右侧 mini browser 的 Study / Review 步骤更贴近真实学习页：左侧明确为上方视频/音频与下方字幕区域，右侧改为笔记标题、章节、正文和来源锚点结构。
- 官网首页右侧 mini browser 演示改为输入、处理、对照学习/复查、导出的真实产品阶段，并移除窗口顶部重复的工作区/来源锚点 meta 条。
- 官网首页右侧 mini browser carousel 改为可探索的步骤控件：降低圆角、强化步骤标题，支持 hover/focus/click 查看某一步，并放慢自动轮播节奏。
- 官网首页全页背景新增更明显的细磨砂纹理层，让浅色和暗色页面底色本身有材质感，而不只依赖卡片表面。
- 官网首页增强背景磨砂/颗粒质感：提高 SVG grain 可见度，并强化 hero、导航、section 和卡片表面的半透明、内高光与 backdrop blur 层次。
- 官网首页右侧 hero visual 改为 mini browser 轮播式产品证明流程，串起上传/粘贴长视频、先生成笔记、对照修正和导出学习资产；首页同步删减解释性文案，header 新增 EN/中与明暗主题切换，来源类型卡片也改为语义不同且对比更清楚的图标。
- 官网首页降低粗字重带来的压迫感：导航、按钮、标签、卡片和动态演示文案改为更轻的 operational typography，并加入轻量 SVG grain 背景纹理提升页面质感。
- 官网首页改为英文优先的 light learning product 方向：首屏突出长视频先生成学习笔记，使用更轻快的圆润 sans 字体气质、细微主题渐变、轻颗粒背景和时间线/笔记稿产品母题。
- 修复官网首页因全局 `body` 锁定滚动而无法上下滑动的问题；首页现在拥有自己的垂直滚动容器，并同步优化中文标题字体层级、学习稿预览质感和 CTA 交互状态。
- 官网首页改为长视频学习笔记定位：首屏突出“先生成高质量笔记再对着视频学习”，并新增学习前后对比、笔记质量依据、来源覆盖边界和可导出学习资产说明。
- 修复本地前端跨端口登录时被浏览器 CORS 拦截而只显示 `Failed to fetch` 的问题；现在本地 Vite 页面会收到明确的账号密码业务错误或正常登录结果。
- 处理记录失败任务卡片不再显示误导性的 100% 进度条，失败阶段和原因说明会与卡片信息区对齐展示。
- 官网首页暗色模式改为统一的暗色表面：导航、辅助文案、次按钮、产品预览和场景区在暗色下不再低对比或反成大面积浅色块。
- 编辑器和处理详情页开始展示 DeepSeek 保守字幕纠错状态：用户可以看到笔记是否基于修正后的字幕生成，并检查已接受修正的原文、修正后文本、原因、置信度和时间点；原始字幕仍保留在转录原文中。
- 后端新增 DeepSeek 保守字幕纠错层：在不覆盖原始转录的前提下，保存高置信字幕修正和 corrected transcript；后续 AI 笔记会优先使用已验证的 corrected transcript。
- 设置页的系统维护区新增 Qwen 作为摘要服务商选项，并可保存百炼 / DashScope API Key；视频笔记截图链路可以用它驱动 Qwen 视觉模型继续执行局部视觉选图。
- 处理详情页的“查看结果”只会在记录确实有可打开结果时出现，并会先载入对应任务结果再进入编辑器，避免失败记录出现无效入口或完成记录打开后显示“未选择结果”。
- 视频笔记的截图插入链路改为“文本模型先提出截图时间窗，Qwen 只复查局部候选帧”；长视频不再为了插图先把全视频交给多模态模型，最终截图仍会经过密度、去重和低价值过滤后才进入笔记。
- 新增公开官网首页，根路径 `/` 用于展示 FluentFlow 的产品主张、Agent 工作流和适用场景；原应用 Dashboard 迁移到 `/app`，视频处理主入口仍为 `/media-text`。
- 侧边栏移除单独的“历史记录”入口，统一由“处理记录”承载进行中任务、失败记录、历史结果和下载入口；旧 `/tasks` 地址会自动进入处理记录页。
- 处理记录任务卡恢复为更紧凑的横向列表形态；完成记录不再重复放大 100% 进度，运行和失败记录继续保留阶段进度与恢复入口。
- 前端页面改为按路由拆分加载，减少首次打开时需要下载和解析的主 JS 体积，消除 Vite 的 500KB chunk 预警。
- 编辑器未选择结果时会引导用户进入处理记录选择已有结果，不再跳回开始处理或上传入口。
- 编辑器的“笔记正文”默认显示为渲染后的可读笔记，可切换到编辑态修改 Markdown 正文；修改后会自动保存，下载摘要和飞书导出会使用编辑后的内容。
- 视频链接任务失败后会保留为失败记录，开始处理页和首页会提示“已保存在处理记录里”，处理记录卡片会持续展示失败原因。
- 视频链接提交成功后会自动进入任务处理详情页；实时进度、处理阶段和 Agent 判断统一在同一处查看，输入页不再保留链接任务进度卡片。
- 任务处理详情页移除重复的执行记录模块，顶部负责实时进度，页面主体聚焦 Agent 判断、证据和下一步建议。
- 修复本地执行的视频链接任务提交后跳到处理详情页却显示 `Job not found` 的问题；任务详情会沿用本地执行 scope 并带上刚创建的任务作为初始状态。
- 侧边栏处理记录入口不再直接打开某一个最近任务，而是进入任务工作台。
- 处理记录和任务详情会先用本地已保存的任务快照打开，再静默刷新后端详情，减少已完成任务反复显示“加载判断推进”的体感。
- 侧边栏处理记录入口改为处理详情记录页：每个视频直接作为一张完整处理卡片展示进度、路线、文件信息、失败恢复和结果入口，不再通过“查看详情”跳到单任务详情页。
- 修复视频链接下载中从处理记录进入详情页时可能退回“排队中 0%”的问题；详情页会沿用下载阶段、字节进度和文件大小，并避免静默刷新用旧任务壳覆盖实时快照。
- 处理详情页完成态会收起重复进度、模型配置、耗时、文件大小和阶段胶囊，只保留复查结果与可用产物；运行态继续显示必要进度，失败态优先显示恢复提示。
- 处理详情页移除右侧重复的生成产物下载区，完成态顶部说明改回按当前记录来源、处理路线和结果构成生成，不再显示通用“结果可以复查”文案。
- 处理详情页的判断卡片改为展示已写入任务结果的原始判断字段，包括结论、原始说明、原始依据、影响和警告，减少状态标签、置信度和 chip 对用户筛选判断内容的干扰。
- 处理详情页的判断卡片进一步收敛为可读摘要，直接展示结论、原因、依据和影响内容，不再把“原始说明 / 原始依据 / 影响”等字段名作为正文前缀。
- 处理详情页的“判断材料类型”卡片只保留材料结论和核心依据，不再重复展示来源、时长、语言判断和默认处理说明。
- 处理详情页不再把“选择处理路线”“决定字幕呈现方式”和“选择笔记生成方式”作为 Agent 判断卡片展示；路线、字幕模式和笔记模式收敛到顶部概览或内部记录。
- 处理详情页顶部概览会用紧凑标签展示实际使用的高保真笔记模式，不再为笔记模式单独占用一张判断卡。
- 处理详情页不再把“判断笔记生成结果”作为 Agent 判断卡片展示；笔记字数改为顶部概览里的紧凑标签。
- 处理详情页完成态顶部标题改为当前视频或素材标题，并降低标题字号；不再用通用“这条记录处理完成”占据主标题。
- Agent 工作流单条任务卡片改为显示具体日期和时间，不再显示“几分钟前 / 几小时前”这类相对时间。
- Agent 工作流完成任务标题下方改为展示处理耗时和占原时长比例，不再重复显示“已完成”。
- Agent 工作流完成任务的材料类型会从结果计划和判断记录中补齐；旧记录缺少判断字段时显示“学习材料”，不再误显示“待判断”。
- Agent 工作流顶部统计卡改为展示历史记录总数，不再展示累计文件大小。
- 处理详情页顶部“来源”卡片优先展示平台来源，例如 Bilibili、YouTube 或抖音；本地任务会显示本地视频、音频或字幕文件，不再默认显示空泛的“素材”。
- 处理详情页的本地转写路线会显示为“medium 模型”，设置页移除本地 STT 模型选择；本地 faster-whisper 固定使用 medium，旧的 large-v3 配置也会回落到 medium。
- 处理详情页顶部任务概览恢复更完整的信息密度，常驻显示进度、来源、处理路线、结果构成和文件信息，但不再恢复重复的大步骤列表。
- 处理详情页运行态会把当前阶段作为主标题展示，例如“转录中”，并把进度和处理路线放到标题下方说明；结果卡片不再重复显示阶段状态。
- 处理详情页顶部概览进一步移除完成态产物胶囊、运行态视频保存提示和结果数量卡片，避免与结果页和顶部说明重复。
- 处理详情页移除底部“下一步 / 复查结果”面板，避免完成任务重复展示复查提示和取消任务动作。
- 处理详情页把“判断材料类型”从大判断卡片收敛为顶部紧凑标签，只保留材料类型和核心依据，判断记录区不再为这条信息占用主内容空间。
- 处理详情页把“判断材料类型”并入顶部任务概览信息区，材料依据允许换行完整展示，不再用省略号截断。
- 处理详情页完成态顶部标题改为单行省略，并移除标题下方重复的“这是一条记录...”说明，减少完成任务概览区的文字噪音。
- YouTube 链接任务失败时会区分“没有可用字幕”和“原视频下载受 YouTube 客户端校验限制”，后台任务不再只显示 PO Token、SABR 或 403 等技术错误。
- 从 Agent 工作流入口进入具体任务详情时，侧边栏会高亮 Agent 工作流，不再误高亮历史记录。
- 编辑器播放器只会使用与当前结果文件名和大小匹配的本地源文件；切换历史记录时不再复用上一条记录的缓存视频。
- 处理详情页静默刷新后会继续保留笔记模式和字数标签；后端任务详情正式返回 note 模式与字数，避免初始缓存标签几秒后消失。
- 开始处理页和视频转写与总结页的最近活动卡片会优先打开可复查的编辑器；非完成记录进入对应任务详情，不再把具体活动点击带到历史记录列表。
- Agent 工作流详情页顶部统一使用任务进度卡；运行中记录会显示当前任务、转录进度、已用时间、文件大小、转录路线、摘要模式和取消任务入口，完成记录也保持同一套布局。
- 本地执行的 YouTube 字幕链接任务会在抓到字幕后继续生成笔记，不再因为后台转交字幕文件时缺少本地执行标记而被账号登录拦截。
- 后台队列的内部请求使用稳定本地 token，并保留账号任务归属，避免已登录用户的视频链接字幕任务在生成笔记时被误判为未登录。
- 账号登录态导致的 AI 笔记失败会显示“重新登录后重试”等可执行提示，不再把 `HTTP 401` 原始错误直接暴露给用户；本地转录路线也会说明“本地只负责转录，AI 笔记仍使用账号和模型服务”。
- 编辑器的视频复查区改为时间轴字幕列表，点击时间即可跳转对应片段；播放位置会按任务记忆，文本校对和视频复查共用同一进度状态，顶部修改记录只展示当前仍有效的字幕差异。
- 编辑器的视频复查区会跟随“中英对照 / 原始字幕”切换显示；双语模式下视频下方字幕同步展示原文和中文翻译。
- 已登录账号选择本地 faster-whisper 处理链接时，任务归属会继续使用账号身份，不再被本地执行标记覆盖成 `local-single-user` 导致 AI 笔记阶段误报未登录。
- 常见任务失败原因会统一转成结构化诊断和下一步建议，覆盖账号、额度、平台限流、文件缺失、转录配置、AI 笔记和飞书导出，不再优先把原始技术报错甩给用户。
- 处理详情页减少重复的历史记录入口，移除无效的判断统计卡，并提高暗黑模式下判断卡片的辅助文字对比度。
- 修复 Agent 工作流暗黑模式下判断卡片说明文字、证据标签和影响说明过暗的问题，统一使用可生成的灰白色文本类。
- 首页最近活动和历史记录页统一使用同一批账号/本机任务缓存和排序规则；进入历史记录会先展示缓存，再后台刷新，减少空白加载和两边记录不一致。
- 视频转写与总结页的最近记录模块统一命名为“最近活动”，并复用开始处理页的空状态文案。
- 历史记录页的刷新、取消和删除操作会优先保留本地刚发生的操作结果，避免轮询请求把已取消或已删除记录短暂带回列表。
- 历史记录页不再把缓存中的真实任务强制标成“缓存完成态”，避免先显示“已完成”再被后端纠正为“失败”的状态跳变。
- 失败的视频链接记录会在保留原始链接时显示“重新处理”，可一键重新提交同一材料。
- 导航中的“后台任务 / 任务管理”改为“历史记录”，让该入口承担历史查看、失败记录、下载产物和少量进行中任务恢复，而不是暴露系统后台机制。
- 版本更新页的“最后更新”和最近记录增加分钟级时间，时间来自 changelog 文件修改时间或条目自带时间，不再只显示日期。
- Agent 接入 / MCP 面板在内容超过弹窗高度时可以正常滚动，不再卡在首屏看不到下方配置。
- 本地前端运行在任意临时端口时，Agent 接入 / MCP 创建 Key 会自动请求本机后端，不再误发给前端服务导致 `Method Not Allowed`。
- 编辑器文本校对的时间列会和字幕首行对齐；视频复查字幕列表收紧行高和字号，减少单条字幕占用空间。

### 维护者变化

- 新增 Word 导出格式参考文档，记录当前 `.doc` HTML 兼容方案、字体槽位和表格布局约束，并标明后续商业版应升级到原生 `.docx` 生成。
- 视频截图链路新增 `visual_key_moments` 结果层，用于保存适合学习复查但不应插入正文的关键画面；`visual_evidence` 继续只表示已进入正文的高置信视觉证据，`frame_artifacts` 保持诊断/原始候选语义。
- 本地运行数据默认目录统一到系统应用数据目录，维护脚本、备份/恢复脚本、部署自检和 Codex 导出脚本改用同一套 runtime path；旧仓库内 `data/`、`backend/data/` 和视频缓存先通过迁移脚本复制，迁移验证后保留 14 天再清理。
- 前端 `AppProvider`、`DropdownMenu` 和后端 `request_scope` / `cloud_proxy` 抽象已接入主入口，`shared.jsx` 和 `server_helpers.py` 只保留兼容转发层，减少半迁移状态带来的上下文噪音。
- 飞书导出新增用户 OAuth 基础层：账号库会保存账号级 Feishu connection，后端可生成授权 URL、校验 callback state、刷新用户 token，并支持 `/export-lark` / Agent export 显式使用 `user_oauth` 路径以用户身份写入文档；旧维护者 OpenAPI 和本机 lark-cli 路径继续保留。
- 官网首页拆出 content、styles 和 HeroProofDemo 局部模块，让 landing route 只保留页面编排、语言/主题状态和 section 组合，便于后续商业官网扩展。
- 官网首页右侧 hero proof demo 拆成独立的步骤控件、四个阶段组件和配置数组，后续调整输入、处理、学习复查与导出步骤时不再把状态机、动画和全部 JSX 混在一个组件里。
- 新增 `fluentflow-homepage-design` 项目 skill，用于官网首页审校和改版前判断，并将 Huashu Design 方向探索纳入流程，避免通用设计 skill 把首页带回深色工具页、泛 AI 模板或不适合中文学习产品的字体方向。
- 部署自检中的视频截图插图检查改为要求百炼 / DashScope 视觉选择 Key，而不再强制主摘要服务商必须设置为 Qwen；DeepSeek/OpenAI 生成文本笔记时也可配合 Qwen 视觉模型完成局部截图选择。
- 新增 `docs/git_checkpoint_workflow.md` 作为 Git checkpoint 单一操作手册；`AGENTS.md`、执行任务 brief 和 git workflow skill 只保留索引，避免多处规则互相冲突。
- 执行任务完成后默认创建本地 checkpoint commit；如果验证未跑、工作仍在探索、或脏改动边界不清，则必须先拆分或说明未提交原因。
- 任务结果新增笔记正文保存接口 `PATCH /jobs/{task_id}/summary`，保存后继续更新既有 `summary_markdown` 字段和 Markdown artifact；Agent API / MCP 读取任务包时无需新增工具即可拿到最新正文。
- Agent 工作流入口调整不改变 Agent API / MCP 合约；它只选择已有任务详情页，任务包字段和 MCP 工具无需同步扩展。
- 新增 Agent 执行任务卡入口机制，帮助主对话和执行对话在开工前明确范围、验证、parity、changelog 和 commit 边界。
- 新增 FluentFlow 项目级协作 Skills，覆盖任务卡拆分、前端变更和视频来源链路排查的重复执行流程。
- 新增 Agent/MCP eval 起步机制，用离线 mock 用例和可选 backend e2e 命令记录工具调用、耗时、任务状态、诊断和任务包摘要。
- UI 设计系统补充截图检查规则，并将多处页面级约束改为按页面职责描述，减少一次性 bug 修复规则对后续 UI 迭代的干扰。
- 强化 Git 工作流规则：非琐碎编辑前必须检查 dirty worktree，发现无关改动时先拆分、checkpoint 或换 clean worktree，避免继续叠加未提交改动。
- 项目 Agent 说明补全关键结构、验证清单和隐私边界，并新增根目录 `.env.example`，让本地配置入口清晰但不泄露真实凭据。
- 新增工作流设计系统文档，沉淀首页、历史记录、处理详情、编辑器、失败恢复和任务状态口径的长期执行规则。
- change-control 检查新增文档治理提醒：自动检查 `AGENTS.md` 行数、引用文档是否存在、UI/工作流改动是否需要复查对应长期文档，并允许提交 `.env.example` 模板。
- change-control 检查新增工作单元大小提醒：当单次变更跨过多文件或作用域时，提示先拆 staged plan，避免单轮 Agent 执行过大。
- 后端新增 `task_snapshot` 任务状态读模型，`/jobs`、`/jobs/{task_id}` 和处理详情接口会附带统一状态、当前步骤、失败原因、下一步动作、产物和处理路线，供后续前端统一读取。
- 前端任务映射开始优先读取 `task_snapshot`，首页最近活动、历史记录、处理详情入口和当前任务卡会优先使用统一状态、当前步骤、失败原因和下一步动作，再回退到旧字段。
- 处理详情页保留顶部核心进度概览，移除重复的大步骤时间线；`task_snapshot.steps` 只作为状态、失败和恢复逻辑的数据来源，不再默认渲染成占据主内容的通用流水线。
- 新增当前版本宏观计划文档，明确本版本目标、边界、主路径、阶段顺序和每次执行的拆分规则；旧 `next_execution_plan.md` 保留为历史上下文。
- 优化工作流、Agent/MCP 和任务卡文档的表达口径，减少页面名、平台名和旧提交策略等过窄规则对后续迭代的干扰。
- 优化结果 schema 和事件日志文档：补齐 Job Payload 结构，保留平台特定采集策略字段，并将事件日志从阶段性埋点说明收敛为长期诊断与统计口径。

### 数据 / 口径变化

- Result Payload v2 新增 `transcript_correction_status`、`transcript_correction`、`transcript_corrections`、`corrected_transcript_text`、`corrected_segments` 和 `note_generation_transcript_source`；原始 `transcript_text`、`raw_segments`、`display_segments` 不会被纠错层覆盖。

### 注意事项

- 升级需要重新构建前端并重启后端；浏览器如仍命中旧 bundle，需要强制刷新。
- 如启用飞书用户 OAuth、百炼 / DashScope 视觉截图、DeepSeek 字幕纠错或迁移后的运行数据目录，需要确认服务器环境变量和迁移状态与运维文档一致。

## v0.2.0｜2026-06-29｜Agent 工作流与云端转录基础版

### 用户可见变化

- 云端转录主路线切换到 ElevenLabs Scribe；Azure Batch 仅保留为 legacy 兼容路径。
- 开始处理页支持在已有转录任务运行时继续添加新音视频进入后台队列。
- 历史记录页统一展示账号任务、本机任务、失败任务和已取消任务，并支持删除已完成、失败或已取消记录。
- 视频链接任务会在主页和历史记录中显示解析、下载、保存、转录和摘要进度，标题优先展示真实视频标题。
- 编辑器支持“重生笔记”和“重新转录”二次确认，避免误触覆盖当前笔记或转录。
- 编辑器新增文本校对 / 视频复查切换；有原视频和时间戳时，可在视频下方编辑当前字幕并跳转上一句 / 下一句。
- 编辑器左侧可保存并复用播放音频，刷新后仍可恢复音频对照和重新转录来源。
- 英文材料默认保留英文原文转录，并可在翻译成功时生成中英双语字幕；摘要仍直接生成中文笔记。
- 字幕导入明确为“导入字幕生成笔记”，支持 SRT、VTT、TXT 和 Markdown，并纳入账号额度与提交频率保护。
- AI 笔记自动选择接入真实规划，会基于材料统计、开头、中段和结尾样本判断直接生成或高保真笔记。
- 长材料新增章节覆盖笔记能力：先抽证据、规划章节、逐章生成，再做覆盖检查，适合课程和讲座类材料。
- 笔记正文支持显示 Agent 选中的视频关键截图，并可隐藏或显示截图，减少对正文阅读的干扰。
- 视频关键截图会经过密度、重复画面和低价值页面过滤；封面、目录、纯标题页和过渡帧不会挤占笔记。
- 下载 Markdown、PDF、Word 时会保留已选截图；飞书 OpenAPI 导出会尝试把本地截图上传为文档图片块。
- Agent 工作流页从旧处理设置表单改为任务解释页，展示当前素材的材料判断、路线选择、笔记策略、影响和复查点。
- Agent 接入面板支持创建、查看和撤销 API Key；新建 Key 会自动填入 Claude Code / Codex 的 MCP 配置示例。
- 本地和云端 Agent/MCP 接入统一使用 API Key，明文 Key 只在创建成功后显示一次。
- 编辑器 AI 摘要底部下线“生成详情”和重复元信息，解释入口收敛到“查看 Agent 工作流”。
- 登录 / 访问门禁、关于与协议、提示词模板弹窗和设置维护区统一到当前 FluentFlow 中性工作流 UI 语言。
- 左下角“关于与协议”补齐服务条款、隐私政策和版本更新入口；版本更新页读取项目 changelog 并展示具体条目。
- 关于与协议页面改为自带滚动容器，长内容不会再被应用根层锁住。
- 首页主标题从“今天想创作些什么呢”改为“今天想记录些什么呢”，更贴近转录、整理和复查学习资料。
- 输入视频链接后的当前任务标题会先显示平台和视频 ID，不再把 B 站跟踪参数当标题；取消任务按钮文案更明确。
- Bilibili 链接解析不再使用 miuistore 第三方兜底；解析失败时提示上传本地视频或配置浏览器 cookies 后重试。
- YouTube 链接会优先抓取可用字幕生成笔记，避免长视频下载阶段出现 403、429 或超时后无法产出笔记。
- 视频转写与总结页顶部操作区在较窄宽度下保持单行排布，避免模式切换和查看记录按钮互相挤压。

### 维护者变化

- 新增 `/agent/v1` Agent 数据链路，支持提交链接/转录文本、等待任务、获取任务包、读取诊断、重生笔记和导出飞书。
- 新增本地 MCP Server 和检查脚本，让 Claude Code、Codex 等外部 Agent 可以通过稳定 API 操作 FluentFlow。
- 新增 SQLite API Key 存储，服务端只保存哈希，并允许 `X-FluentFlow-Access-Token` 或 `Authorization: Bearer` 携带 Key 调用 `/agent/v1`。
- `/agent/v1` 纳入后端 API 路由前缀和账号鉴权边界，API Key 会绑定到账号 scope 或本地 client scope。
- 新增 Agent / MCP parity 规则：未来修改用户工作流时，需要同步判断 Agent API、MCP 工具、任务包字段、schema、文档和测试是否更新。
- 前端任务/历史映射抽到 `frontend/src/lib/jobMappers.js`，设置、转录路线和云端配置判断抽到 `frontend/src/lib/settingsModel.js`。
- Agent 任务的“重生笔记”和“导出飞书”动作抽到 `backend/core/agent_task_actions.py`，router 只保留请求入口。
- 本地/云端执行通道判断拆到后端 request scope 和前端 local execution 模块，减少 path/header 规则复制。
- 前端新增 api client 与 task state 口径，`cached_only` 成为正式前端任务状态，旧 `__cacheOnly` 只作为历史兼容输入。
- 本地运行产物默认迁移到系统应用数据目录，并新增显式迁移脚本和统一 runtime path 管理。
- 后台任务队列改为持久化任务步骤模型，上传、访客试用和视频链接任务先写入可恢复步骤，再由 worker 领取执行。
- 视频链接任务新增 YouTube 字幕优先路径和资产策略 metadata：`yt-dlp` 逐个字幕语言尝试，成功后走现有 `/summarize-transcript-file` 链路；无字幕时才回退媒体下载，并按视频时长/估算大小调整下载预算。
- 新增取消任务、删除任务、播放音频补写、中文翻译补写等后端接口，支撑编辑器和后台任务恢复动作。
- 新增离线笔记质量评测、STT 候选比较、MCP 检查和日常 change-control 检查脚本。
- 新增产品版本管理基础：`VERSION` 作为唯一版本源，后端 `/version` 和前端构建配置暴露版本、Git commit 与 schema 信息。
- 新增 release prepare / release check 流程、release checklist 和版本策略文档，区分日常 checkpoint 与正式 release gate。
- 补充关于二级页面 UI、一致性文案、Agent 工作流说明、结果 schema、MCP 集成、运维和回归检查的维护文档。

### 数据 / 口径变化

- 任务结果新增 `result_schema_version = "2"`；新写入保存长期字段 `raw_segments` / `display_segments`。
- `segments`、`bilingual_segments`、`translated_segments_zh`、`cleaned_segments` 等旧字段只作为历史兼容读取入口。
- 任务结果新增 `processing_plan`、`source_language`、`subtitle_mode`、`translation_status`、提示词模板元数据和 note mode plan 元数据。
- 视频来源 metadata、结果 JSON 和本地历史条目新增 `raw_title` / `display_title`；`source_filename` 继续保留为机器存储文件名。
- 新增 SQLite `job_steps` 表；`jobs` 继续作为用户可见任务状态，`job_steps` 作为后台 worker 的执行队列。
- 飞书导出新增长期字段 `lark_export_route` / `larkExportRoute`，旧 `lark_via_cli` / `larkViaCli` 继续兼容。
- 热词审阅旧能力长期下线，相关端点保留 410 Gone 墓碑响应；本机历史候选扫描与账号导入旧路线也保留 410 兼容端点。

### 注意事项

- 升级需要重新构建前端并重启后端；浏览器如仍命中旧 bundle，需要强制刷新。
- 生产环境需要确认 `FLUENTFLOW_ALLOWED_STT_PROVIDERS`、`FLUENTFLOW_DEFAULT_STT_PROVIDER`、ElevenLabs API Key、飞书 OpenAPI 凭证和对象存储/截图配置。
- 本版本包含 result schema、任务步骤表、运行产物路径和历史标题语义变化；旧数据应通过兼容读取和启动补齐逻辑继续可读。
- 正式部署、打 tag 和发布 manifest 仍需在 clean worktree 上按 release checklist 执行。

## 2026-06｜公开试用前的账号、隔离与额度边界

### 用户可见变化

- 支持账号模式，云服务器公开试用时建议使用账号登录，而不是只靠访问码。
- 未启用账号模式时，前端会生成设备级 `client_id`，不同浏览器或设备默认看不到彼此的历史任务。
- 云服务器公开模式下，普通用户不需要理解本地 STT、Azure Key、Blob SAS、pyannote token、飞书 App Secret 等维护者配置。

### 维护者变化

- 增加上传大小、队列数量、单设备活跃任务数、全站活跃任务数、每日任务数、每日上传量和 IP 提交频率限制。
- 增加部署自检脚本和清理脚本，用于上线前检查配置、上线后控制磁盘增长。
- 云服务器推荐使用 Azure Batch 作为默认 STT Provider；本地 faster-whisper 保留为开发和本地使用路径。

### 数据 / 口径变化

- 任务历史、下载、转录稿编辑和取消任务会按账号或设备作用域隔离。
- 设备级隔离不是正式账号系统；用户清空浏览器数据或换浏览器后，会变成新的设备身份。

### 注意事项

- 公开试用建议开启 `FLUENTFLOW_AUTH_MODE=accounts`。
- 如果只启用访问码或设备级隔离，不要对外承诺“账号级历史同步”。

## 2026-06｜Azure Batch 云转录主路径

### 用户可见变化

- 支持通过云端 Azure Batch 处理长音视频转录。
- 任务页会展示上传 Blob、提交任务、等待 Azure、下载结果等阶段。
- Azure Batch 阶段不伪造 STT 百分比，只展示真实阶段和等待状态。

### 维护者变化

- 需要配置 Azure Speech 地址、Key 和 Blob container SAS URL。
- 音视频会先压缩为 MP3，再上传到 Blob，避免本地 Whisper 专用 WAV 体积膨胀。
- 提供 `scripts/check_azure_batch_transcription.py` 做 dry-run 和真实 smoke test。

### 数据 / 口径变化

- `stt_completed.metadata.stt_provider` 可区分本地 faster-whisper、Azure Batch 和历史 Azure Fast。
- STT 性能报告必须按 provider、设备、模型、速度档和语言口径分组。

### 注意事项

- Azure Batch 的等待时间包含上传、网络、云端排队和处理时间，不能和本地推理耗时直接混成一个平均数。

## 2026-06｜转录编辑与修改记录沉淀

### 用户可见变化

- 转录结果页支持编辑分段文本。
- 用户修改后的转录稿会自动保存，避免人工校对结果丢失。
- 修改记录会沉淀为本地资产，包含修改前后文本和相邻上下文，便于后续 STT 质量评估。

### 维护者变化

- 编辑稿和修改记录保存在本地数据目录中，属于用户内容，不应提交到 Git。
- 清理策略需要同时考虑源文件、产物、编辑稿和修改记录。

### 数据 / 口径变化

- 转录编辑记录可以用于质量评估和错误类型分析，但不能直接包装成“用户满意度”或“笔记可用率”。

### 注意事项

- 目前没有完整人工校对计时入口，不应从编辑行为推断校对完成耗时。

## 2026-06｜长字幕高保真笔记生成

### 用户可见变化

- 笔记生成支持自动选择、直接长上下文和高保真笔记三种模式。
- 长字幕会先剥离 SRT/VTT 时间码、序号和标签，再按内容段落重组，减少无关格式占用上下文。
- 高保真模式会先分段提取证据，再生成终稿，适合长课程和信息密度高的材料。

### 数据 / 口径变化

- 摘要事件会记录请求模式、实际采用模式、分段数和覆盖检查信息。
- `summary_completed` 只代表 AI 摘要真实成功，不代表最终质量一定可用。

### 注意事项

- 不要把“下载摘要”或“导出飞书”写成“用户认可笔记质量”。

## 2026-06｜第一阶段事件日志

### 维护者变化

- 新增 best-effort SQLite 事件日志：`data/fluentflow_events.sqlite`。
- 提供事件导出脚本，支持 JSON / CSV。

### 数据 / 口径变化

- 记录真实发生的导入、音频提取、STT、转写就绪、摘要完成/失败/跳过、飞书导出、下载、取消和任务终态。
- 不保存完整转写文本或完整 Markdown，只保存长度、耗时、状态、错误原因和链接。
- 不记录当前产品中没有真实入口的行为，例如评分、校对完成、笔记可直接使用。

### 注意事项

- 历史 localStorage 快照不能写入事件表，不能伪造成未来事件日志。

## 2026-06｜本地 STT 可取消与真实进度口径

### 用户可见变化

- 本地 faster-whisper 转录任务支持真正取消，取消后会终止后端 STT 子进程。
- 转录阶段展示模型加载、准备音频、等待第一段、接收片段等状态。
- 第一段 segment 产出前不伪造进度百分比。

### 维护者变化

- STT 从线程池迁移到独立子进程，牺牲部分模型热缓存速度，换取资源可控。

### 数据 / 口径变化

- `stt_completed` 记录设备环境、模型、速度档、语言、实时倍率和模型加载耗时。
- 本地 STT 耗时必须说明设备和模型口径。

### 注意事项

- SSE 连接断开不等于用户取消任务；用户点击取消才是明确取消行为。
