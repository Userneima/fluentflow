# FluentFlow Changelog

本文档记录 FluentFlow 面向使用者和维护者的版本变化。它和 `docs/product_growth_log.md` 的分工不同：

- `product_growth_log.md` 记录为什么改、做过哪些判断和取舍。
- `changelog.md` 记录这次改了什么、影响什么用户、上线时要注意什么。

每次有可感知的产品变化、部署变化、数据口径变化或破坏性调整时，都应该补一条。不要把还没落地的计划写成已发布能力。

## 记录格式

```md
## YYYY-MM-DD｜一句话版本主题

### 用户可见变化
- ...

### 维护者变化
- ...

### 数据 / 口径变化
- ...

### 注意事项
- ...
```

如果某一类没有变化，可以省略。注意事项里要写清是否需要重启后端、重新构建前端、迁移数据、更新环境变量或通知用户。

## Unreleased｜本地桌面与云端账号历史同步

### 用户可见变化

- 登录后如果检测到本机旧历史，会在开始页提示导入到当前账号。
- 导入需要用户手动确认；确认后会上传转录文本和摘要，导入完成后最近活动从云端账号历史显示。
- 本地桌面快捷方式默认把账号、任务、上传、历史和额度 API 转发到 `https://fluentflow.icu`，后续桌面和网页使用同一份云端工作区。

### 维护者变化

- 新增 `docs/local_cloud_sync_plan.md`，明确桌面本地前端、云端 API、旧历史导入和隐私边界。
- 后端新增 `POST /account/import-history`，用于把确认导入的本机历史写入当前登录账号。
- 后端新增 local-only `GET /local-history/candidates`，仅用于桌面云工作区读取本机 SQLite 候选历史。
- 导入后的历史作为 completed account job 保存，并生成可下载文本/摘要 artifacts。

### 数据 / 口径变化

- 导入历史会生成新的 `imported_*` task id，不复用本地旧 task id，避免跨账号或跨设备主键冲突。
- 导入记录不包含原始音视频文件，`source_file_available` 和 `playback_audio_available` 为 false。
- 重复导入按原始 task id 和 source fingerprint 去重。

### 注意事项

- 需要重新构建前端资源。
- 需要重新安装或更新桌面 `FluentFlow.app` 快捷方式，才能启用默认云端工作区。
- 本次不会自动上传旧记录；用户需要在界面中点击导入。

## Unreleased｜UI 暗黑模式规范固化

### 维护者变化

- 新增 `docs/ui_design_system.md`，把 FluentFlow 的工具型 UI、暗黑模式、语义色 token、字体层级、表面层级和组件约束固化为项目规范。
- `AGENTS.md` 新增 UI / 暗黑模式路由规则，要求后续创建、重构或视觉调整 FluentFlow 页面前先读取该规范。

### 注意事项

- 本次只改变维护文档和协作规则，不改变运行时代码、用户数据、主题 token 或部署配置。

## Unreleased｜服务器部署触发规则

### 维护者变化

- 新增 `docs/server_deploy_workflow.md`，把“上传服务器 / 部署到服务器 / 上线”的标准执行流程固化为文档。
- `AGENTS.md` 新增服务器部署触发规则：当用户使用约定触发词时，Codex 应先读取部署流程文档，然后自动完成本地验证、相关文件提交、推送 GitHub、服务器部署脚本执行和健康检查。
- 明确 Routine 部署不使用 `nano` 或手动服务器改文件，GitHub 继续作为代码事实来源。

### 注意事项

- 本次只改变协作规则和维护文档，不改变运行时代码、服务器配置或部署脚本。
- Codex 不会后台常驻监听；触发发生在用户对话中明确提出部署意图时。

## Unreleased｜薄管理员额度维护界面

### 维护者变化

- 前端新增管理员入口，只对 `admin` 角色显示。
- 管理员页支持查看账号列表、当前余额、最近余额流水。
- 支持手动增减用户处理额度，并要求填写调整原因，可选填写订单号、截图编号等凭证备注。

### 注意事项

- 本次只落地早期手动充值所需的最小后台，不包含支付订单、二维码生成、用户角色修改、封号、密码重置或套餐配置。
- 上线需要重新构建前端并重启后端服务，后端权限仍以 `/admin/*` 接口校验为准。

## Unreleased｜大改前计划文档规则

### 维护者变化

- `AGENTS.md` 新增“大改前先产出计划文档”的协作规则。
- 当修改触及账号、支付、额度、数据、任务队列、部署、运维、跨前后端流程等高语义风险区域时，应先创建或更新聚焦的方案文档，再按文档拆分落地。
- 小范围 bug 修复、文案调整、视觉微调和局部实现仍可直接处理，避免把所有任务流程化。

### 注意事项

- 本次只改变维护协作规则，不改变产品功能、数据库、部署配置或运行时行为。

## Unreleased｜编辑器标题信息密度优化

### 用户可见变化

- 编辑器页顶部文件名会限制显示长度，避免长字幕文件名挤占操作区和内容区。
- 对系统生成的长数字/哈希前缀，主标题显示时会自动弱化，只保留更有辨识度的文件名主体。
- 鼠标悬浮在标题上时仍可查看完整原始文件名。

### 注意事项

- 本次只改变前端显示，不改变真实文件名、导出文件名、任务数据或历史记录。

## Unreleased｜长期维护文档补齐

### 维护者变化

- 新增 Changelog，用于记录面向用户和上线维护的版本变化。
- 新增运维手册，用于记录启动、重启、日志、清理、故障排查和上线 smoke test。
- 新增回归检查清单，用于每次修改后验证核心链路没有退化。
- `AGENTS.md` 新增版本记录规则：关键修改、大量修改、部署/数据/认证/额度/存储/恢复等语义变化后，需要同步更新本 Changelog。

### 注意事项

- 本组变更只补维护文档和协作规则，不改变业务逻辑、历史数据、事件日志或部署配置。

## 2026-06｜公开试用前的账号、隔离与额度边界

### 用户可见变化

- 支持账号模式，云服务器公开试用时建议使用账号登录，而不是只靠访问码。
- 未启用账号模式时，前端会生成设备级 `client_id`，不同浏览器或设备默认看不到彼此的历史任务。
- 云服务器公开模式下，普通用户不需要理解本地 STT、Azure Key、Blob SAS、pyannote token、飞书 App Secret 等维护者配置。

### 维护者变化

- 增加上传大小、队列数量、单设备活跃任务数、全站活跃任务数、每日任务数、每日上传量和 IP 提交频率限制。
- 增加部署自检脚本和清理脚本，用于上线前检查配置、上线后控制磁盘增长。
- 云服务器推荐使用 Azure Batch 作为默认 STT Provider；本地 faster-whisper 保留为开发和本地使用路径。

### 数据 / 口径变化

- 任务历史、下载、转录稿编辑和取消任务会按账号或设备作用域隔离。
- 设备级隔离不是正式账号系统；用户清空浏览器数据或换浏览器后，会变成新的设备身份。

### 注意事项

- 公开试用建议开启 `FLUENTFLOW_AUTH_MODE=accounts`。
- 如果只启用访问码或设备级隔离，不要对外承诺“账号级历史同步”。

## 2026-06｜Azure Batch 云转录主路径

### 用户可见变化

- 支持通过云端 Azure Batch 处理长音视频转录。
- 任务页会展示上传 Blob、提交任务、等待 Azure、下载结果等阶段。
- Azure Batch 阶段不伪造 STT 百分比，只展示真实阶段和等待状态。

### 维护者变化

- 需要配置 Azure Speech 地址、Key 和 Blob container SAS URL。
- 音视频会先压缩为 MP3，再上传到 Blob，避免本地 Whisper 专用 WAV 体积膨胀。
- 提供 `scripts/check_azure_batch_transcription.py` 做 dry-run 和真实 smoke test。

### 数据 / 口径变化

- `stt_completed.metadata.stt_provider` 可区分本地 faster-whisper、Azure Batch 和历史 Azure Fast。
- STT 性能报告必须按 provider、设备、模型、速度档和语言口径分组。

### 注意事项

- Azure Batch 的等待时间包含上传、网络、云端排队和处理时间，不能和本地推理耗时直接混成一个平均数。

## 2026-06｜转录编辑与修改记录沉淀

### 用户可见变化

- 转录结果页支持编辑分段文本。
- 用户修改后的转录稿会自动保存，避免人工校对结果丢失。
- 修改记录会沉淀为本地资产，包含修改前后文本和相邻上下文，便于后续 STT 质量评估。

### 维护者变化

- 编辑稿和修改记录保存在本地数据目录中，属于用户内容，不应提交到 Git。
- 清理策略需要同时考虑源文件、产物、编辑稿和修改记录。

### 数据 / 口径变化

- 转录编辑记录可以用于质量评估和错误类型分析，但不能直接包装成“用户满意度”或“笔记可用率”。

### 注意事项

- 目前没有完整人工校对计时入口，不应从编辑行为推断校对完成耗时。

## 2026-06｜长字幕高保真笔记生成

### 用户可见变化

- 笔记生成支持自动选择、直接长上下文和高保真笔记三种模式。
- 长字幕会先剥离 SRT/VTT 时间码、序号和标签，再按内容段落重组，减少无关格式占用上下文。
- 高保真模式会先分段提取证据，再生成终稿，适合长课程和信息密度高的材料。

### 数据 / 口径变化

- 摘要事件会记录请求模式、实际采用模式、分段数和覆盖检查信息。
- `summary_completed` 只代表 AI 摘要真实成功，不代表最终质量一定可用。

### 注意事项

- 不要把“下载摘要”或“导出飞书”写成“用户认可笔记质量”。

## 2026-06｜第一阶段事件日志

### 维护者变化

- 新增 best-effort SQLite 事件日志：`data/fluentflow_events.sqlite`。
- 提供事件导出脚本，支持 JSON / CSV。

### 数据 / 口径变化

- 记录真实发生的导入、音频提取、STT、转写就绪、摘要完成/失败/跳过、飞书导出、下载、取消和任务终态。
- 不保存完整转写文本或完整 Markdown，只保存长度、耗时、状态、错误原因和链接。
- 不记录当前产品中没有真实入口的行为，例如评分、校对完成、笔记可直接使用。

### 注意事项

- 历史 localStorage 快照不能写入事件表，不能伪造成未来事件日志。

## 2026-06｜本地 STT 可取消与真实进度口径

### 用户可见变化

- 本地 faster-whisper 转录任务支持真正取消，取消后会终止后端 STT 子进程。
- 转录阶段展示模型加载、准备音频、等待第一段、接收片段等状态。
- 第一段 segment 产出前不伪造进度百分比。

### 维护者变化

- STT 从线程池迁移到独立子进程，牺牲部分模型热缓存速度，换取资源可控。

### 数据 / 口径变化

- `stt_completed` 记录设备环境、模型、速度档、语言、实时倍率和模型加载耗时。
- 本地 STT 耗时必须说明设备和模型口径。

### 注意事项

- SSE 连接断开不等于用户取消任务；用户点击取消才是明确取消行为。
