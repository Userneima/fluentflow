# -----------------------------------------------------------------------------
# System dependency (not installable via pip): FFmpeg
# You must install ffmpeg on the OS and have `ffmpeg` on PATH.
#   - macOS:  brew install ffmpeg
#   - Ubuntu: sudo apt install ffmpeg
# moviepy / faster-whisper / many pipelines expect it for decode/encode.
# -----------------------------------------------------------------------------

faster-whisper>=1.0.0
opencc-python-reimplemented>=0.1.7
moviepy>=1.0.3
openai>=1.0.0
lark-oapi>=1.0.0
fastapi>=0.115.0
httpx>=0.27.0
python-multipart>=0.0.9
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
alibabacloud-oss-v2==1.3.2
alibabacloud-credentials==1.0.10

# Optional speaker diarization:
#   pip install -r requirements-speaker.txt
